# 멋쟁이 타자처럼 - Style Reference

> 축제 방문객이 라디오 채널을 선택하고 준비된 문장을 입력해 기록을 겨루는 타자 게임. 레트로한, 빈티지한, 정돈된.

**Theme:** light

**Platform:** laptop 1920x1080 - 축제 부스 현장 기기에서 F11 전체화면으로 사용, 모바일 미지원

> 이 문서에 정의되지 않은 색상, 크기, 간격, 모서리 값은 임의로 추가하지 않습니다.

## 0. Product Experience

- 서비스 콘셉트: 음악과 라디오 분위기를 적용한 타자 기록 경쟁
- 핵심 문구: **당신의 타자로 완성하는 오늘의 방송**
- 핵심 흐름: 메인 -> 참가자 확인 -> 채널 선택 -> 게임 안내 -> 카운트다운 -> 타자 게임 -> 결과 -> 랭킹 또는 재도전
- 한 번의 플레이는 축제 부스에서 짧게 참여하고 바로 결과를 확인하는 경험으로 구성한다.
- 목표 플레이 시간은 약 1분이며, 문장은 한 번에 하나씩 보여준다.
- 세 채널은 같은 화면 구조, 게임 규칙, 결과 화면을 사용한다.
- 채널이 바뀌면 제시 문장, 비교 랭킹, 테마 포인트 컬러만 바뀐다.

## 1. Colors

| 이름 | 값 | 쓰는 곳 |
| --- | --- | --- |
| 홈 배경 | `images/background.png` | 홈 화면의 배경 이미지, 전체 색 비중의 60% |
| 메인 컬러 | `#432F2E` | 전체 색 비중의 30%를 차지하는 주 색상 |
| 흰색 | `#F4F5F0` | 타자기 종이, 밝은 카드와 패널, 어두운 영역 위 글자 |
| 검은색 | `#221F1D` | 밝은 영역의 기본 글자, 아이콘, 1px 테두리 |
| 서브 포인트 | `#EFF7FD` | 메인 컬러 영역의 타자기 키 입력 하이라이트 |
| 오류 | `#B42318` | 오류 테두리, 오류 문구, 오류 아이콘에만 사용하는 공통 기능 색상 |

### Theme point colors

| 테마 | 포인트 컬러 | 사용 규칙 |
| --- | --- | --- |
| 성결 멋사 ON AIR | `#730C02` | 서비스 대표 포인트이자 해당 테마의 주 포인트 |
| 캠퍼스 주파수 | `#7B8055` | 해당 테마 안에서 모든 포인트 역할을 담당 |
| 페스티벌 라디오 | `#E77B49` | 해당 테마 안에서 모든 포인트 역할을 담당 |

### Color composition and rules

- 색 비율은 홈 배경 이미지 60% : 메인 컬러 30% : 현재 테마의 포인트 컬러 10%로 사용한다.
- 세 포인트 컬러 중 메인 포인트 컬러는 `#730C02`이다.
- 홈과 공통 화면에서는 `#730C02`를 대표 포인트 컬러로 사용한다.
- 특정 테마에 진입하면 해당 테마의 포인트 컬러가 주 버튼, 선택 상태, 진행 막대, 채널 표시 등 모든 포인트 역할을 이어받는다.
- 포인트 컬러는 테마별로 하나만 사용한다. 서로 다른 테마의 포인트 컬러를 한 화면 또는 한 테마에 섞어 쓰지 않는다.
- 오류 색 `#B42318`은 포인트 컬러가 아닌 `semantic-error` 토큰이며 모든 테마에서 공통으로 사용한다.
- 오류 색은 넓은 배경이나 장식에 사용하지 않고 오류 테두리, 문구, 아이콘에만 제한한다.
- 오류 상태는 색상만으로 전달하지 않고 오류 아이콘과 설명 문구를 함께 제공한다.
- `#EFF7FD`는 반드시 `#432F2E`와 겹쳐 사용한다. 단독 배경, 단독 텍스트, 또는 다른 색 위에는 사용하지 않는다.
- `#EFF7FD`의 주 용도는 타자 게임에서 현재 누른 키를 보여주는 입력 피드백이다.
- 순백 `#FFFFFF`과 순검정 `#000000`은 사용하지 않는다.

## 2. Typography

### Font families

- 메인 제목, 섹션 제목, 카드 제목: `Maru Buri`
- 본문 및 기본 UI: `Maru Buri`
- Fallback: `system-ui, sans-serif`

봄바람체는 메인 홈의 서비스 제목 `멋쟁이 타자처럼`에만 사용한다. 제목, 본문, 부가 정보, 버튼, 입력창을 포함한 나머지 모든 UI 텍스트에는 `Maru Buri`를 사용한다.

### Type scale

| 역할 | 크기 | 굵기 | 용도 |
| --- | --- | --- | --- |
| 화면 제목 | `56px` | `600` | 화면의 가장 큰 제목, Maru Buri |
| 섹션 제목 | `36px` | `600` | 섹션 제목, Maru Buri |
| 카드 제목 | `24px` | `600` | 카드 또는 목록 제목, Maru Buri |
| 본문 | `18px` | `400` | 기본 설명 텍스트, Maru Buri |
| 부가 정보 | `14px` | `400` | 날짜, 상태, 위치 등, Maru Buri |

- 글자 굵기는 `400`과 `600`만 사용한다.
- `600`은 버튼, 선택 상태, 기록처럼 강조가 필요한 기본 UI 텍스트에만 사용한다.

### Game sentence

- 제시 문장과 사용자가 입력하는 글자: `32px / 400 / line-height 1.6`
- 글꼴: `Maru Buri`
- 한 문장은 최대 2줄까지 표시한다.
- 문장이 길어도 화면 폭에 맞추기 위해 글자 크기를 자동으로 줄이지 않는다.

## 3. Spacing

- 사용할 간격 단위: `4px`, `8px`, `12px`, `16px`, `24px`, `32px`, `48px`, `64px`
- 위 목록 외의 중간 간격 값은 사용하지 않는다.
- 화면 좌우 여백: `64px`
- 카드 안쪽 여백: `24px`
- 카드와 카드 사이: `24px`

## 4. Radius

- 버튼: `4px`
- 카드: `8px`
- 입력창: `4px`
- 원형 요소: `999px`
- 알약 모양 버튼과 과도하게 둥근 컨테이너는 사용하지 않는다.

## 5. Elevation

- 깊이는 `#221F1D` 1px 테두리와 배경색 차이로만 표현한다.
- 카드와 일반 UI 컨테이너에는 그림자를 가급적 사용하지 않는다. 꼭 필요한 경우에도 정보 구조를 해치지 않는 최소한의 표현만 허용한다.
- 카드 구분 방식: 배경과 카드의 색상 차이 + `1px` 테두리
- `#EFF7FD` 타자기 키 입력 하이라이트의 글로우는 필요한 피드백이므로 사용할 수 있다. 그 밖의 그림자는 꼭 필요한 경우에만 최소한으로 사용한다.

## 6. Components

### Required components

- `ON AIR` 상태 표시
- 게임 시작 전 3초 카운트다운
- 세 가지 라디오 채널 선택 요소
- 닉네임 및 전화번호 입력 폼
- 게임 방법과 완료 조건 안내 영역
- 제시 문장과 타자 입력 영역
- 오타 위치 표시
- 경과 시간 표시
- 현재 문장 수 표시
- 문장 완료에 따라 채워지는 방송 진행 막대
- `방송 완료` 결과 상태
- 이번 기록, 개인 최고 기록, 최고 기록 갱신 여부, 현재 순위 요약
- 카테고리별 랭킹 탭과 순위 목록
- 시작 및 완료 효과음 음소거 제어

### ON AIR indicator

| 상태 | 이미지 |
| --- | --- |
| 방송 켜짐 | `src/assets/images/ON AIr_light on .png` |
| 방송 꺼짐 | `src/assets/images/ON Air_light off .png` |

- 게임 화면에서는 `ON AIR` 표시를 화면 중앙 상단에 배치한다.
- 게임이 시작되면 방송 켜짐 이미지를 표시한다.
- 게임이 진행되는 동안 방송 켜짐 상태를 유지한다.
- 마지막 문장이 완료되어 게임이 끝나면 방송 꺼짐 이미지로 교체한다.
- 홈 화면에서는 방송 시작 전 상태로 방송 꺼짐 이미지를 표시한다.
- 에셋의 원본 비율을 유지하고 찌그러뜨리거나 글자를 잘라내지 않는다.

### Typewriter key feedback

- 게임 화면의 타자기는 하단 중앙에 배치한다.
- 타자기 최대 너비는 `1200px`이며 원본 비율을 유지한다.
- 타자기 이미지는 화면의 핵심 오브젝트로 크게 보여주고 자르거나 찌그러뜨리지 않는다.
- 종이와 입력 문장은 타자기 중앙축에 맞춰 정렬한다.
- 타자기 키 자체를 움직이거나 눌린 형태로 변형하지 않는다.
- 사용자가 누른 물리 키의 위치에 `#EFF7FD` 하이라이트를 표시한다.
- 하이라이트는 키 테두리 링, 옅은 내부 빛, 은은한 외부 글로우로 구성한다.
- 키 안의 글자가 계속 읽히도록 불투명한 색으로 키 전체를 덮지 않는다.
- `keydown`에 즉시 점등하고 `keyup` 후 약 100ms 안에 자연스럽게 사라진다.
- 한글 입력에서도 조합된 글자가 아니라 `event.code`를 기준으로 실제 누른 키의 위치를 표시한다.
- 하이라이트가 놓이는 타자기 영역은 메인 컬러 `#432F2E` 계열의 배경 또는 프레임과 함께 구성한다.

### Buttons

- 공통 화면 주 버튼: `#730C02` 배경 + `#F4F5F0` 글자
- 테마 내부 주 버튼: 현재 테마의 포인트 컬러 배경 + `#F4F5F0` 글자
- 보조 버튼: `#F4F5F0` 배경 + `#221F1D` 글자 + `#221F1D` 1px 테두리
- 버튼 글자: `Maru Buri`, `18px`, `600`
- 버튼 높이: `52px`
- 버튼 모서리: `4px`
- 버튼에는 그림자를 가급적 사용하지 않으며, 필요한 경우에도 최소한으로 사용한다.
- 한 화면의 주 버튼은 하나만 사용하고 나머지 동작은 보조 버튼이나 글자 링크로 표현한다.
- 글자 링크는 밝은 영역에서 `#221F1D`, 어두운 영역에서 `#F4F5F0`을 사용한다.
- 글자 링크는 기본 상태부터 1px 밑줄을 표시하고 글자와 밑줄 사이는 `4px` 띄운다.
- 글자 링크의 호버 및 포커스 상태에서는 밑줄만 `2px`로 강조한다.
- 글자 링크에 불필요한 화살표나 장식 아이콘을 붙이지 않는다.
- 메인 화면 동작: `게임 시작`, `랭킹 보기`
- 결과 화면 동작: `500원으로 재도전`, `랭킹 보기`

### Channel identity assets

| 채널 | 포인트 컬러 | 채널 카드 옆 LP 이미지 | 제목 옆 아이콘 이미지 |
| --- | --- | --- | --- |
| 성결 멋사 ON AIR | `#730C02` | `src/assets/images/LP_red.png` | `src/assets/images/mic_red.png` |
| 캠퍼스 주파수 | `#7B8055` | `src/assets/images/LP_green.png` | `src/assets/images/headset_orange.png` |
| 페스티벌 라디오 | `#E77B49` | `src/assets/images/LP_orange.png` | `src/assets/images/radio_green.png` |

- 채널 선택 영역은 음반 커버 또는 플레이리스트 포스터 같은 구성을 사용한다.
- 화면에는 현재 선택된 테마의 LP 하나만 표시한다.
- 최초 진입 시 `성결 멋사 ON AIR`가 기본 선택이며 `src/assets/images/LP_red.png`를 표시한다.
- 테마를 선택하면 LP 이미지, 제목 옆 아이콘, 포인트 컬러, 채널 정보가 해당 테마 값으로 함께 변경된다.
- 선택된 LP는 시계 방향으로 8초에 한 바퀴씩 일정한 속도로 계속 회전한다.
- LP 회전은 가속과 감속 없이 `linear` 타이밍으로 무한 반복한다.
- 운영체제에서 모션 감소를 설정한 경우 LP 회전을 정지한다.
- LP 이미지는 작은 아이콘이나 독립된 썸네일로 배치하지 않는다.
- LP 이미지를 채널 선택 영역 왼쪽에 크게 고정하고, 원의 일부가 컨테이너 경계에서 잘리도록 배치한다.
- LP는 왼쪽 영역의 약 절반을 차지하며 전체 원판이 모두 보일 필요는 없다.
- 컨테이너에는 `overflow: hidden`을 적용해 LP가 경계에서 깔끔하게 잘리도록 한다.
- 채널 이름, 설명, 문장 정보, 진행 상태 등 주요 콘텐츠는 LP 오른쪽 영역에 정렬한다.
- 채널 제목 옆에는 매핑된 아이콘 이미지를 이모지처럼 함께 배치한다.
- 제목 옆 아이콘의 표시 높이는 제목 글자 크기와 같은 `1em`으로 맞춘다.
- LP 이미지와 제목 아이콘은 원본 비율을 유지하며 찌그러뜨리지 않는다. LP는 의도적인 카드 경계 크롭만 허용한다.

### Cards and inputs

- 일반 카드: `#F4F5F0` 배경 + `#221F1D` 1px 테두리 + `8px` 모서리. 그림자는 가급적 사용하지 않는다.
- 채널 카드 기본 상태: `#F4F5F0` 배경 + `#221F1D` 1px 테두리
- 채널 카드 선택 상태: 배경색은 유지하고 현재 테마 포인트 컬러의 `2px` 테두리 적용
- 채널 카드의 선택 상태에서는 배경색 변경이나 그림자 추가를 가급적 피하고, 현재 테마 포인트 컬러의 테두리를 우선 사용한다.
- 채널 카드의 LP와 오른쪽 콘텐츠 영역이 겹쳐 글자 가독성을 해치지 않도록 한다.
- 선택되지 않은 테마의 LP 이미지를 동시에 노출하지 않는다.
- 홈 화면의 세 채널 요약 카드는 하나의 큰 공통 배경 박스로 묶지 않고 각각 독립된 카드로 표시한다.
- 카드 제목에는 말줄임표나 잘라내기를 사용하지 않으며, 필요한 경우 줄바꿈해 전체 제목을 표시한다.
- 입력창 기본: `#F4F5F0` 배경 + `#221F1D` 글자, 라벨, 1px 테두리
- 입력창 높이: `52px`
- 입력창 좌우 안쪽 여백: `16px`
- 입력창 모서리: `4px`
- 입력창 포커스: 현재 테마 포인트 컬러 2px 테두리
- 입력창 오류: `#B42318` 2px 테두리 + `#B42318` 오류 문구 + 오류 아이콘
- 입력창에는 그림자를 가급적 사용하지 않는다.
- 칩 / 필터 기본: `#F4F5F0` 배경 + `#221F1D` 글자 + `#221F1D` 1px 테두리
- 칩 / 필터 선택: 현재 테마 포인트 컬러 배경 + `#F4F5F0` 글자
- 칩 / 필터 글자: `Maru Buri`, `16px`, `600`
- 칩 / 필터 높이: `44px`
- 칩 / 필터 좌우 안쪽 여백: `16px`
- 칩 / 필터 모서리: `4px`
- 칩 / 필터에는 그림자를 가급적 사용하지 않는다.
- 선택 상태는 포인트 컬러와 함께 체크 표시 또는 상태 아이콘을 제공해 색상만으로 구분하지 않는다.

### Screen inventory

| 화면 | 반드시 포함할 내용 |
| --- | --- |
| 메인 | 꺼진 ON AIR 에셋, 서비스 소개, 게임 시작, 랭킹 보기, 참여 비용, 상품, 마감 안내 |
| 채널 선택 | 세 채널의 이름, 설명, 문장 예시 |
| 참가자 확인 | 닉네임, 전화번호, 개인정보 수집 안내 및 동의, 무료 참여 또는 이용 권한 상태 |
| 게임 안내 | 입력 방법, 완료 조건, 기록 기준 |
| 게임 | 중앙 상단의 켜진 ON AIR 에셋, 제시 문장, 입력 영역, 경과 시간, 현재 문장 수, 방송 진행 막대 |
| 결과 | 꺼진 ON AIR 에셋, 이번 기록, 개인 최고 기록, 최고 기록 갱신 여부, 현재 순위, 재도전, 랭킹 보기 |
| 랭킹 | 카테고리별 탭, 순위, 닉네임, 완료 시간, 1등과 2등 강조 |
| 운영진 | 참가자 조회, 결제 확인, 이용 권한 부여, 기록 무효 처리, 마감 관리 |

### Home composition

- 홈 화면의 빈티지 인상은 LP, 의미 없는 상태 스티커, 낡은 질감 같은 장식에 의존하지 않고 정보 구조와 타이포그래피로 만든다.
- 상단은 꺼진 `ON AIR` 에셋과 서비스명, 축제 부스 정보가 하나의 가는 구획선 위에 놓이는 방송국 마스트헤드 구조를 사용한다.
- 메인 소개와 참여 비용 및 상품 안내는 좌우로 나누고, 참여 안내는 인쇄된 방송 안내표처럼 가는 구획선으로 정리한다.
- 세 채널은 하나의 큰 공통 배경 박스 없이 독립된 방송 편성표 카드 세 장으로 표시한다.
- 채널 카드에는 채널 번호, 채널명, 제목 아이콘, 설명, `5문장 · 약 1분` 정보와 주파수 눈금을 표시한다.
- 실시간 TOP 3는 중첩된 밝은 카드 목록 대신 메인 컬러 패널 위의 가는 구획선과 기록 행으로 구성한다.
- 홈 화면에는 장식용 LP와 `BROADCAST READY` 같은 별도 상태 스티커를 사용하지 않는다.
- 홈 화면에서는 분위기를 위한 영문 라벨과 반복 설명을 추가하지 않는다. 제목, 참여 조건, 채널명, 소요 시간, 주요 동작, 랭킹 상태처럼 사용자가 바로 판단하는 정보만 남긴다.
- 홈의 채널 카드에는 별도 설명 문장을 반복하지 않고 채널 번호, 채널명, `5문장 · 약 1분` 정보만 표시한다.

## 7. Guidelines

### Do

- 라디오 기기와 방송 화면을 연상시키는 구조를 사용한다.
- 레트로하고 빈티지한 인상을 유지하되 구성과 표면은 정돈되고 깨끗하게 표현한다.
- 게임의 문장과 입력 영역은 큰 글씨와 명확한 대비로 가독성을 확보한다.
- 제시 문장과 입력 글자는 항상 같은 크기와 줄간격을 유지한다.
- 시작할 때 `ON AIR` 표시와 카운트다운을 제공한다.
- 키 입력 시 타자기의 해당 키에 `#EFF7FD` 점등 피드백을 제공한다.
- 문장 완료 상태는 방송 진행 막대로 명확히 보여준다.
- 결과 화면은 `방송 완료` 상태와 기록 정보를 함께 보여준다.
- 탭과 필터의 선택 상태는 색상 외의 체크 표시 또는 상태 아이콘으로도 구분한다.
- 오류 상태는 `#B42318` 색상과 오류 아이콘, 설명 문구를 함께 사용한다.
- 채널별 화면 구조는 동일하게 유지하고 포인트 컬러와 콘텐츠만 변경한다.
- 채널 카드의 LP 이미지는 카드 내용 옆의 대표 비주얼로 배치하고, 제목 옆에는 제목과 같은 `1em` 높이의 채널 아이콘 이미지를 배치한다.
- 테마 안에서는 주 버튼과 모든 강조 상태를 해당 테마 포인트 컬러로 통일한다.
- 채널 선택 카드에는 채널별 LP 이미지와 제목 옆 아이콘 이미지를 지정된 매핑대로 사용한다.
- 시작 및 완료 효과음에는 음소거 기능을 제공한다.
- 작은 스티커형 그래픽은 화면 분위기를 보조하는 장식으로 소량 사용할 수 있다.
- 스티커는 주요 콘텐츠, 버튼, 입력 영역, 기록 정보를 가리지 않도록 배치한다.

### Don't

- 서로 다른 채널의 포인트 컬러를 한 화면에 섞지 않는다.
- 테마 내부에서 대표 포인트 `#730C02`를 해당 테마 포인트 컬러와 함께 경쟁시키지 않는다.
- 과도하게 따뜻하고 포근한 분위기로 표현하지 않는다.
- 얼룩, 먼지, 찢김, 과한 노이즈처럼 지저분하거나 낡아 보이는 질감을 사용하지 않는다.
- 버튼, 카드, 입력창을 과도하게 둥글게 만들거나 알약 형태로 만들지 않는다.
- 키 입력 피드백에 이동, 눌림, 흔들림, 크기 변화 애니메이션을 사용하지 않는다.
- `#EFF7FD` 하이라이트로 키의 글자를 가리지 않는다.
- 긴 문장을 맞추기 위해 게임 문장의 글자 크기를 임의로 축소하지 않는다.
- 카드와 일반 UI 컨테이너에 드롭 섀도우를 과도하게 사용하지 않는다.
- 채널 카드 선택 상태에 배경색 변화나 그림자를 과도하게 사용하지 않는다.
- 순백 `#FFFFFF`과 순검정 `#000000`을 추가하지 않는다.
- 그라데이션은 가급적 사용하지 않으며, 필요한 경우에도 빈티지하고 레트로한 분위기를 보조하는 범위에서 최소한으로 사용한다.
- 글래스모피즘과 반투명 유리 효과를 사용하지 않는다.
- `#EFF7FD` 키 입력 하이라이트를 제외한 네온과 글로우 효과를 사용하지 않는다.
- 과한 3D 효과와 입체 그림자를 사용하지 않는다.
- 팔레트에 없는 임의 색상을 추가하지 않는다.
- `#B42318`을 포인트 컬러나 장식 색으로 사용하지 않는다.
- 스티커를 반복적으로 사용하거나 정보보다 더 눈에 띄게 만들지 않는다.
- 채널에 따라 게임이나 결과 화면의 구조를 다르게 만들지 않는다.
- 랭킹 화면에 참가자의 전화번호를 노출하지 않는다.
- 현재 순위를 최종 수상 결과처럼 표현하지 않는다.

## 8. Responsive Behavior

- 기준 화면: 노트북 `1920x1080`
- 모든 화면은 `1920x1080` 해상도의 `F11` 전체화면을 기준으로 설계한다.
- 일반 브라우저 창 크기와 모바일 화면은 대응 범위에 포함하지 않는다.
- 모바일 대응은 제공하지 않는다.
- 키보드와 마우스 입력을 기준으로 구성한다.
- 타자기 이미지는 최대 `1200px` 너비 안에서 원본 비율을 유지하고, LP는 의도한 컨테이너 경계 크롭만 허용한다.
- 우선 사용 환경은 축제 부스이며, 한 명이 짧게 플레이하고 즉시 결과를 확인할 수 있어야 한다.

## 9. Open Decisions

- 현재 미결정 항목 없음
